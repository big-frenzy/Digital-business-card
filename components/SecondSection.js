import React from "react"

export default function SecondSection () {
    return (
        <section>
        <h2>Interests</h2>
        <p>Skilled in building responsive, scalable, and user-friendly interfaces, adept at translating designs into efficient, responsive code</p>
        </section>
    )
}